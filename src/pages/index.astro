---
import { getCollection } from "astro:content";
import slugify from "slugify";
import Layout from "~layouts/Layout.astro";

const notesList = await getCollection("notes");
const worksList = await getCollection("works");
---

<Layout title="Haikel Ilham Hakim">
  <div
    class:list={[
      "flex flex-col items-start justify-start w-full",
      "md:text-start",
    ]}
  >
    <section>
      <h1 class="mb-0">Haikel Ilham Hakim</h1>
      <p id="description" class="text-justify mb-0">
        Hello! I'm Haikel. Usually, people call me <b>Haikel/Kel/Ekel</b>.
        Currently doing internship as a Fullstack Engineer
        <a
          href="https://dibimbing.id/"
          target="_blank"
          rel="noopener noreferrer">@dibimbing.id</a
        >, playing around with Astro JS, GraphQL, Node JS, Chakra UI, and Next
        JS.
        <b>
          <i>Keep it simple</i>
        </b>, is one of my principles while crafting something.
        <br />
        Apart from coding, I do <b>Photography</b> with my camera. Mostly I took
        street photos, nature, historic place, and cosplayer at an event. I love
        this hobby because it gives me many perspective about life, and of course,
        It&#39;s fun.
      </p>
    </section>
    <section class="w-full mt-8">
      <h2 class="mb-6 mt-0">Experience</h2>
      <div class="space-y-3">
        <div>
          <div
            class="w-full flex flex-col sm:flex-row justify-start items-start sm:justify-between sm:items-center"
          >
            <h3 class="mt-0 mb-0">Dibimbing.id - Internship</h3>
            <span class="rounded-sm mt-1.5 font-semibold sm:mt-0 text-sm"
              >Sep 2024 - Present</span
            >
          </div>
          <p class="mt-3 mb-0 sm:mt-1">Fullstack Engineer</p>
        </div>
        <div>
          <div
            class="w-full flex flex-col sm:flex-row justify-start items-start sm:justify-between sm:items-center"
          >
            <h3 class="mt-0 mb-0">Infinite Learning - Apprenticeship</h3>
            <span class="rounded-sm mt-1.5 font-semibold sm:mt-0 text-sm"
              >Feb 2024 - Jun 2024</span
            >
          </div>
          <p class="mt-3 mb-0 sm:mt-1">Web Development & UI/UX Design</p>
        </div>
        <div>
          <div
            class="w-full flex flex-col sm:flex-row justify-start items-start sm:justify-between sm:items-center"
          >
            <h3 class="mt-0 mb-0">[Undisclosed] - Freelance</h3>
            <span class="rounded-sm mt-1.5 font-semibold sm:mt-0 text-sm"
              >Nov 2023 - Dec 2023</span
            >
          </div>
          <p class="mt-3 mb-0 sm:mt-1">Frontend Web Developer</p>
        </div>
        <div>
          <div
            class="w-full flex flex-col sm:flex-row justify-start items-start sm:justify-between sm:items-center"
          >
            <h3 class="mt-0 mb-0">We Up Indonesia - Contract</h3>
            <span class="rounded-sm mt-1.5 font-semibold sm:mt-0 text-sm"
              >Oct 2022 - Dec 2022</span
            >
          </div>
          <p class="mt-3 mb-0 sm:mt-1">Frontend Web Developer</p>
        </div>
      </div>
    </section>
    <section class="w-full mt-8">
      <h2 class="mb-6 mt-0">Featured Works</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 grid-rows-1 w-full gap-4">
        {
          worksList.slice(0, 4).map((item) => (
            <div
              class:list={[
                "p-4 w-full flex flex-col bg-white dark:bg-base-0",
                "flex-wrap border border-dashed border-base-4 dark:border-base-5",
              ]}
            >
              <a
                href={`/works/${slugify(item.data.title, { lower: true })}`}
                class:list={[
                  "cursor-pointer text-lg font-bold",
                  "!no-underline",
                ]}
              >
                <h3 class="font-extrabold mt-0 line-clamp-2 mb-0 text-2xl">
                  {item.data.title}
                </h3>
              </a>
              <p class="mt-4 mb-0">{item.data.description}</p>
            </div>
          ))
        }
      </div>
    </section>
    <section class="w-full mt-8">
      <h2 class="mb-6 mt-0">Latest Notes</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 grid-rows-1 w-full gap-4">
        {
          notesList.slice(0, 4).map((item) => (
            <div
              class:list={[
                "p-4 w-full flex flex-col bg-white dark:bg-base-0",
                "flex-wrap border border-dashed border-base-4 dark:border-base-5",
              ]}
            >
              <a
                href={`/notes/${slugify(item.data.title, { lower: true })}`}
                class:list={[
                  "cursor-pointer text-lg font-bold",
                  "!no-underline",
                ]}
              >
                <h3 class="mt-0 line-clamp-2 mb-0 font-extrabold text-2xl">
                  {item.data.title}
                </h3>
              </a>
              <p class="mt-2 mb-0">{item.data.description}</p>
            </div>
          ))
        }
      </div>
    </section>
  </div>
</Layout>
