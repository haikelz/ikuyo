---
import { format } from "date-fns";

type Props = {
  title: string;
  description: string;
  slug: string;
  createdAt?: Date;
  type: "works" | "notes";
  tags: string[];
};

const { title, description, slug, createdAt, type, tags } = Astro.props;
---

{
  type === "notes" ? (
    <div
      class:list={[
        "flex w-full flex-col relative p-8",
        "rounded-none",
        "bg-transparent border border-border/70 hover:border-border/80 transition-colors",
        "text-foreground",
      ]}
    >
      <div class="relative z-10">
        {createdAt ? (
          <span class="font-medium text-sm text-muted-foreground">
            {format(createdAt, "LLLL d, yyyy")}
          </span>
        ) : null}
        <a
          href={`/notes/${slug}`}
          class:list={["mt-3 block cursor-pointer", "no-underline!"]}
        >
          <h3 class="mb-0 mt-0 line-clamp-2 text-2xl font-medium tracking-tight text-foreground">
            {title}
          </h3>
        </a>
        <p class="mb-0 mt-3 line-clamp-3 text-muted-foreground text-base leading-relaxed">
          {description}
        </p>
        <div class="flex justify-start mt-6 items-center gap-3 w-fit flex-wrap">
          {tags.map((item) => (
            <a
              class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors"
              href={`/tags/${item}`}
            >
              <span>#{item}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
  ) : type === "works" ? (
    <div
      class:list={[
        "flex w-full flex-col relative",
        "rounded-none",
        "bg-transparent border border-border/70 hover:border-foreground/25 transition-colors",
        "text-foreground",
      ]}
    >
      <div class="flex w-full flex-col p-6 sm:p-8 relative z-10">
        <p
          class="mb-4 mt-0 font-mono text-xs font-medium uppercase tracking-[0.12em] text-muted-foreground"
        >
          Project
        </p>
        <a
          href={`/works/${slug}`}
          class:list={["cursor-pointer block", "no-underline!"]}
        >
          <h3 class="mb-0 mt-0 line-clamp-2 text-2xl font-medium tracking-tight text-foreground">
            {title}
          </h3>
        </a>
        <p class="mb-0 mt-3 text-muted-foreground text-base leading-relaxed text-pretty">
          {description}
        </p>
        <div class="flex justify-start mt-auto pt-6 items-center gap-x-3 gap-y-2 w-fit flex-wrap">
          {tags.map((item) => (
            <a
              class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors"
              href={`/tags/${item}`}
            >
              <span>#{item}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
  ) : null
}
